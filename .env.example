# Environment template (copy to `.env` for production)
PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=cinestream_db

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
JWT_SECRET=change_me_to_a_long_random_string

# Restrict allowed origins in production (comma-separated or single domain)
# CORS_ORIGIN=https://yourdomain.com
